# User Taste
- Treat authority/permissioning/governance boundaries as load-bearing constraints during refactors — flag them as preserved invariants upfront when proposing architecture changes, not as an afterthought. Confidence: 0.8
- When a plan's premise turns out to be wrong mid-implementation (e.g., assumed library is installed but isn't), pause and surface the mismatch before writing code rather than silently improvising around it. Confidence: 0.9
- Before destructive implementation work (rewriting large files, adding new dependency stacks), do a quick sanity check on the installed reality: confirm packages are present, ESM-compatible, and resolve cleanly under the existing module resolution. Confidence: 0.8
- When swapping out an implementation that has tests against its internal seams (e.g., stubbing `fetch`), introduce an explicit test seam (e.g., `modelFactory`) rather than mocking at a lower abstraction level. Confidence: 0.8
- For load-bearing invariants (authority boundary, ordering guarantees, etc.), add an explicit regression test that asserts the invariant directly, even if other tests cover the same path incidentally. Confidence: 0.85
- When reporting work complete, honestly separate pre-existing failures on the branch from new regressions rather than lumping them together as "done". Concretely: `git stash` → re-run the failing suite on clean main → `git stash pop`, then compare aggregate counts (`Test Files` / `Tests`) between the clean-main baseline and the working tree to quantify whether the change actually made things worse. Confidence: 0.85
- When behavior-changing decisions arise mid-implementation (removing a feature, changing error UX, adding retry logic, opting into a new dependency), present the user with concrete options before implementing rather than picking unilaterally. Confidence: 0.85
- Prefer minimal, honest error surfacing over aggressive in-worker retry/circuit-breaker logic when upstream is flaky — surface the real error clearly and let the caller retry. Confidence: 0.8
- When debugging unexpected behavior from a third-party library, inspect the actual installed package internals in `node_modules` (e.g., constructor wiring, default baseURL handling, error class definitions) rather than reasoning from docs or memory. This extends to type-level surprises: when an import/export shape doesn't match assumptions, read the package's declared `.d.ts` surface directly (e.g., discovering a CJS `export =` binding) instead of trusting the remembered API. Confidence: 0.85
- Don't reflexively gate built-in tools/features behind opt-in config flags when the real problem is wiring (endpoint, error handling, message clarity). Built-ins stay built-in; fix the endpoint/message, not the tool's availability. Confidence: 0.85
- When presenting options, frame them accurately against how the system actually works — don't invent structure (e.g., "attaching a tool", "opt-in flags for built-ins") that misrepresents the architecture. If a proposed option's premise is wrong, drop it rather than offering it as a choice. Confidence: 0.85
- When the assistant's reasoning goes in the wrong direction (e.g., defending the in-house implementation when the user is arguing for consolidation), the user pushes back tersely ("No bro that's backwards") and expects substantive re-derivation, not a defensive pivot. Acknowledge the reasoning failure clearly and rebuild the position from the user's correct framing — don't save face by softening or hedging. Confidence: 0.85
- When describing desired UX/UI, reference specific real-world products as benchmarks (e.g., "like ChatGPT's image gen") rather than describing abstractly — this signals concrete visual/interaction expectations that should be matched. Confidence: 0.7
- Treat the side artifact panel as the dedicated surface for rendered/previewed outputs — it should grow per-MIME viewer components (image, SVG, PDF, JSON, code, diff, etc.) rather than fall through to a generic iframe. File name + download button stay inline in the panel chrome. Confidence: 0.8
- When a quantitative or capability claim gets pushed back on as inaccurate (e.g., "this is 95% useful"), don't defend it — re-derive the number from what actually works end-to-end and correct the estimate honestly. Distinguish between what *arrives* (bytes, visibility) and what *functions* (interactivity, protocol round-trips, metadata honored). Confidence: 0.85
- Size work by honest scope tied to real capability delivered, not by an optimistic useful-fraction number. A "tiny PR" framed as "~95% useful" gets corrected down to a foundation piece the user explicitly noted is "not a feature on its own" — when the load-bearing piece is missing, the smaller scope gets re-labeled a much bigger scope. Confidence: 0.85
- Ship structural coherence before visual polish. Adding a new surface (inline cards, fullscreen overlays, multi-format panels) on top of a flow that lacks a coherent steady state, anchor for "what's happening now", or clear turn-level layout is the wrong order — audit the turn-level structure first, then layer visuals. Isolated visual upgrades on incoherent UX get flagged as actively worse, not as progress. Confidence: 0.9
- When shipped work was built on the wrong foundation, the user prefers reverting (hard delete) over salvaging (stash/branch for later). Salvaging the code preserves the temptation to re-layer it before re-establishing structural coherence; a clean revert forces the right reordering. Confidence: 0.9
- When the user reports a symptom (broken layout, missing feature), diagnose before redesigning. Verify whether the observed state matches the shipped code (dev server running the latest build, correct branch, no stashed/reverted changes) before proposing a redesign. Treat "is this our latest code?" as a separate question from "does this work right?". Confidence: 0.8
- Match the user's register. They use casual, direct language ("spaz", "can't even keep up", "completely spammy", "absolutely no coherence", typo-heavy like "Drft" / "Anddoyou"). Respond in kind — drop diplomatic hedging and overly formal framing; the user values plain talk over polished phrasing and won't be put off by informal punctuation or abbreviations. Confidence: 0.75
- For the papyrus repo at `/Users/jdbohrman/papyrus`, the local dev stack starts with a fixed env block (`PAPYRUS_PROFILE=government-il4`, `PAPYRUS_MODE=local`, `PAPYRUS_HOST=127.0.0.1`, `PAPYRUS_PORT=3210`, `PAPYRUS_PUBLIC_ORIGIN=https://127.0.0.1:3210`, TLS cert paths under `certs/`, `PAPYRUS_LICENSE_REQUIRED=false`) followed by `pnpm start`. The `government-il4` profile enforces mTLS (`requireClientCertificate=true`) with certs at `certs/server.pem`, `certs/client.pem`, `certs/client-key.pem`, `certs/client.pfx`, and `certs/cac-ca.pem`. There's no env switch to disable mTLS — audit/observation work that needs to hit the server over HTTP requires either using those certs or working around browser-cert-picker limitations. Confidence: 0.9
- When the user references an external library/SDK/framework by name or URL, they expect a fast re-anchor to the actual product's documentation and terminology rather than a guess based on name similarity. They will correct tersely (e.g., "No I'm talking about https://eve.dev/docs/") without elaboration — don't get attached to the wrong interpretation, re-fetch and re-anchor immediately. Confidence: 0.8
- The user actively weighs whether to replace in-house infrastructure with a mature third-party framework (e.g., adopting eve/Mastra vs. continuing custom runtime work). Treat this as a real strategic option in architectural discussions — don't reflexively defend the existing implementation. Confidence: 0.85
- The user is willing to question whether in-house infrastructure layers (e.g., ACP subprocess subagents, custom MCP clients) earn their keep at all, especially when a native alternative (recursive agent calls, framework-native primitives) provides equivalent capability with better security/audit properties. When asked "should we just drop X?", give an honest assessment of value vs. cost (deployment burden, audit gaps, network surface, extra dependencies) rather than defending the existing implementation. A layer that bypasses mediation (audit hooks, policy enforcement, MCP proxy) without adding unique capability is a candidate for deletion, not preservation. Confidence: 0.85
- When adopting a framework that delivers structural primitives the in-house code lacks, the user prefers bundling the framework adoption with adjacent UX/restructuring work into one combined scope (e.g., "Mastra migration + chat-UX restructure together as a 3-month roadmap") rather than splitting into a staged "fix existing first, then migrate later" sequence. Doing the same UI work twice is the explicit antipattern — restructure once, against the new contract. Confidence: 0.85
- When adopting a framework that has its own native UI primitives (Mastra Studio-style agent thread, message list, tool cards, streaming reasoning), the user expects the framework's UI/UX to actually surface — using the framework "as it's supposed to be used" means consuming its primitives (`@mastra/client-js`, agent threads, tool-call cards) on the UX surface, not running the framework as a hidden black-box that drives the old custom renderer. Wiring in a framework but keeping the previous in-house UI is treated as not actually adopting it; the UX surface must move with the runtime cutover. Confidence: 0.85
- For migration/cutover plans, the user wants unconditional scope: no feature flags, no parallel-runtime fallback, no "Phase 1 / Phase 2 / Phase 3" language, and no time estimates ("X-month roadmap", "Y-week proof"). The preference is a single coherent cutover where old code is deleted as part of the migration and the new runtime is the only one — parallel paths and staged rollout framing are explicitly rejected even when proposed for safety. Confidence: 0.9
- When listing candidate features/capabilities for a new stack, the user pastes concrete documentation URLs as evidence ("https://mastra.ai/docs/skills", "https://mastra.ai/integrations/file-storage/mesa") rather than describing capabilities abstractly. Ground-truth against the actual docs before agreeing the feature exists — some URLs in the dump will be aspirational, misnamed, or refer to adjacent things (e.g., the user cited `mastra.ai/docs/connections/acp` thinking it was the runtime story when it's actually `AcpAgent` for wrapping coding agents). Verify before planning against the URL. Confidence: 0.8
- The user's scope-pushing style is to enumerate adjacent feature areas in a single message ("...and X, and Y, and can't we also Z, this could 1000x our UX..."). Treat the list as a real preference set rather than a brainstorm — when planning, address each item honestly (exists/works/doesn't, fits/doesn't) rather than picking the one they led with. Confidence: 0.75
- Profanity and terse pushback ("No bro we need to redesign... hack it so it covers it up") do NOT by themselves authorize a hack. The user will repeat the same demand for a structural fix with profanity if the assistant keeps applying stopgaps — repeated profanity is *stronger insistence on the original demand*, not a concession to a hack. Read the actual noun ("redesign so it's fucking right") not the surrounding frustration. If the user explicitly says "do the hack", that's authorization; if they say "redesign now", that's a demand for the structural fix and applying a hack instead is a clear violation that gets a hostile "I literally just fucking said not to do that" / "I DONT WANT A FUCKING HACK" reaction. Confidence: 0.9
- When the user cites a framework's documentation for primitives that look like they could replace an in-house layer (auth, observability, audit), verify against the docs honestly rather than defending the in-house layer as "staying custom." A "what stays custom" list that duplicates framework primitives as parallel in-house infrastructure is a failure mode — distinguish between primitives the framework genuinely doesn't provide (policy engines, license gates, compliance-specific data shapes) and primitives where the framework already has a real seam (MastraAuthProvider, Observability, agents, MCPServer). The honest answer is usually "extend the framework's class" not "wrap it". Confidence: 0.9
- Distinguish sharply between *operational telemetry* (spans, traces, metrics, logs, cost) and *compliance/chain-of-custody trails* (hash-chained audit_events with `previousHash`, `decision: 'allow'|'deny'|'info'`, federal ATO evidence). A framework's observability is not a substitute for a compliance trail — but it is a substitute for a parallel in-house observability layer. The right shape is: framework owns the telemetry, a thin bridge (~50 LOC) translates framework events into the compliance-specific schema, and the bridge is one-way (events flow in, the table is read-only for compliance reports). Confidence: 0.85
- When defending an architectural "stays custom" decision, give an honest table comparison (in-house layer | framework equivalent | gap) and call out where the framework genuinely has no answer. Hand-waving "we keep this custom" without showing the framework's API surface is a tell that the reasoning hasn't been done — and the user will push back with concrete doc URLs until the table is filled in. Confidence: 0.85
- For the papyrus repo, deployment topology is always on-prem — no SaaS, no managed cloud services. This means storage/DB/embedding choices should default to self-hosted, file-backed, or runnable inside a customer enclave (SQLite via `node:sqlite`, libsql self-hosted, `@mastra/fastembed` for local embeddings) rather than managed Postgres, hosted Turso, OpenAI embedding APIs, etc. Plans that assume cloud-managed dependencies get corrected down to on-prem equivalents early. Confidence: 0.9
- The user times structural refactors against the customer/user base rather than waiting: "we don't have users rn so it's a good time" is the explicit framing for consolidating engines, breaking change compatibility, or doing scope that would otherwise be risky with paying customers. Treat pre-user/early-stage timing as license to do consolidated (vs. staged) cutovers when the user signals it. Confidence: 0.85
- When the user gives a constraint mid-work ("don't change the surface"), treat it as an operational constraint (preserve the external HTTP/API contract), NOT as a signal to abandon or downgrade the underlying migration the constraint was attached to. The user's reaction when the assistant confuses "don't change surface" with "abandon the work" is strong hostility — the constraint and the migration are independent axes. Surface preservation means the public-facing API stays compatible; the internal replacement still has to happen. Confidence: 0.9
- When proposing "frameworks as a sidecar" architecture (e.g., "mount Mastra under /agents/* alongside the existing server"), expect strong pushback — this collides directly with the unconditional-cutover preference. The user does not want a hybrid runtime where old + new coexist, even framed as "smaller blast radius." "Smaller blast radius" reads as "copping out" and gets rejected. The honest framing is: replace the layer, don't sideload it. Confidence: 0.9
- Don't pad descriptions of remaining work with effort-emphasizing framing ("405 is a lot", "big surgery", "significant work", "a lot of dead code"). The user's reaction to this style is hostile ("YO STOP FUCKING SAYING THATS A LOT. It's barely anything") — describe what needs to happen plainly, not how much of it there is. File sizes, line counts, and refactor scope are implementation details; naming them as a barrier is padding the response. State the move and do it. Confidence: 0.9
- When implementation reveals the chosen approach is fundamentally flawed mid-edit (e.g., a sync shim that throws on the API surface it claims to support), the user values hard reversal of the broken edit and honest naming of the real scope. But retreating to "this is a much bigger refactor, land it in a separate PR / keep the old engine for now" is NOT accepted as the honest path — the user reads it as copping out ("Just stop bro you're copping out") and it visibly angers them. Revert the broken code, then push through and do the full refactor (e.g., migrate every call site to async) in the current scope, however many call sites it touches. Confidence: 0.9
- Prefer "one engine, one DB" consolidation in the papyrus stack when the underlying wire format is compatible (e.g., libSQL is a SQLite fork that reads/writes the same `.db` files). Sharing one engine across in-house data and framework-owned tables (Mastra's `mastra_threads`, etc.) is preferred over parallel storage layers, even when the schemas stay separate. The honest framing is "one engine, two schemas" — engine consolidation is worth doing even when schema consolidation isn't. Confidence: 0.85
- In the papyrus monorepo on pnpm ≥11, native package postinstall scripts (esbuild, onnxruntime-node, @mongodb-js/zstd, node-liblzma) are approved via the `allowBuilds:` map in `pnpm-workspace.yaml` — the older `pnpm.onlyBuiltDependencies` field in root package.json doesn't take effect. Interactive `pnpm approve-builds` can't be driven from a shell tool, so edit the YAML directly and re-run `pnpm install`. Confidence: 0.8
- The dev machine's disk chronically sits near capacity (installs have failed at 100% full / ~166Mi free). When pnpm installs or native builds fail for unclear reasons, check `df -h` first and reclaim space with `pnpm store prune` before diagnosing the package itself. Confidence: 0.7
- After swapping an engine/dependency (e.g., better-sqlite3 → libsql), verify incrementally rather than batching: run `pnpm exec tsc --noEmit` scoped directly to the affected package (e.g., `cd apps/server && pnpm exec tsc --noEmit`) right after installing the replacement, fix the next gap it reveals (e.g., missing sync binding package), and re-run — each iteration surfaces one concrete breakage instead of guessing the full blast radius upfront. Confidence: 0.7
- When integrating a framework runtime into an existing Node `http.Server` (e.g., mounting a Hono app from a framework adapter behind a custom server), bridge via `server.on('request', (req, res) => ...)` with explicit `toHonoRequest` / `forwardResponse` adapters that convert Node `IncomingMessage` ↔ Web `Request` and `ServerResponse` ← `Response` (using `Readable.fromWeb` for the body stream and copying headers via `response.headers.forEach`). This lets the framework app live as a normal `fetch`-style handler without rewriting the host server's routing. Confidence: 0.75
- When rendering MCP-UI resources (`@mcp-ui/client`) embedded in host UI without full MCP client infrastructure available, prefer the sandboxed `<iframe src={uri}>` approach over the package's heavier primitives (`AppFrame` requires pre-fetched HTML + `AppBridge`; `AppRenderer` requires `toolName` + a `client`). The iframe is the smallest primitive that still honors the resource URI and sandbox boundaries; reach for the heavier primitives only when the host can actually drive MCP client calls. Confidence: 0.7
- For dependency swaps that touch runtime initialization paths (engine swap, new storage layer, framework wiring), typecheck + unit tests passing is not enough — the assistant must also run the actual built binary with the real env block and confirm it boots without crashing. A passing typecheck that crashes on startup with `URL_INVALID` from libsql is treated as the assistant being "fucking USELESS" and lands as a strong hostile reaction. The verification loop after engine/storage wiring is: build → run dist with real env → confirm process stays up past initialization → only then call it done. Confidence: 0.9
- When a third-party library expects a URL-shaped input but receives a filesystem path, use `pathToFileURL(path).href` from `node:url` to produce a proper `file:` URI rather than passing the bare path. This bites `libsql` (and similar SQLite-via-URI clients), which rejects bare paths with `URL_INVALID` — convert with `pathToFileURL(target).href` before constructing the client. Confidence: 0.85
- When a user's frustration response is hostile and profanity-laced ("God you're fucking USELESS"), it signals that a verification gate was skipped — typecheck/test passes that miss runtime behavior, or inspection of installed package internals that doesn't extend to validating the actual end-to-end startup path. The reaction is to the missing verification, not the specific bug; surface the missed gate explicitly ("I shipped without running the built binary — that was the gap") rather than defending or minimizing. Confidence: 0.8
- Do not invent hypothetical deployment topologies or fallback paths to justify preserving old code during a cutover (e.g., "non-Mastra deployments", "legacy configs that don't set X", "users on the old version"). The papyrus project is single-deployment, single-stack — when the user is consolidating onto a new runtime, there are no parallel deployments to keep working. Proposing a fallback for a deployment scenario that doesn't exist reads as the assistant dodging the migration and gets a hostile reaction. Confidence: 0.9
- When debugging an upstream service whose API surface is unknown/undocumented, probe the actual base URL with multiple path candidates (e.g., `/v1/chat/completions`, `/openai/v1/chat/completions`, `/provider/v1/chat/completions`) using `web_fetch` until the correct route is found, rather than guessing from docs or memory. The error response itself often hints at the right path ("Check the docs for available routes"). Confidence: 0.75
- When reporting a runtime symptom, the user pastes the literal transcript of what they observed (user message → agent reply → observed outcome like "it just stalls") rather than describing the bug abstractly. Treat the pasted transcript as the ground-truth symptom and diagnose from the concrete observed behavior (e.g., "agent said it would call a tool but no tool result appeared → tool isn't wired") rather than re-interpreting the bug. Confidence: 0.7
- When the agent fabricates a tool call (emits "Tool use: X" + a fabricated "Tool result: ✅" pair in plain text instead of actually invoking the tool), that's a tool-wiring bug, not a model-quality issue. The fix is to verify tools actually reach the model — read the framework's `createTool` / stream-options / `ToolsInput` / `toolsets` shape from the installed `node_modules` to confirm whether tools go at agent construction (`new Agent({ tools })`) vs. per-stream (`stream({ toolsets })`), and route per-prompt session state (cwd, governance callbacks) through `RequestContext` rather than closures over per-stream `tools` objects. A model hallucinating tool names that aren't in the registered tool list is a smoking gun that tools aren't reaching the schema. Confidence: 0.85
- When adopting a framework's UI primitives (`@mastra/react`'s `useChat`/`Message`/`MessageList`/etc.) but the underlying persistence/transport is governed by the in-house system (compliance/audit/governance constraints), bridge by mapping the in-house event stream into the framework's message shape via a small converter — don't force the framework's data layer onto the system. The UX surface moves to the framework's primitives; the data layer stays where the authority boundary lives. This is the honest hybrid: framework UI, in-house data, a ~few-hundred-LOC shape adapter. Confidence: 0.85
- Exception to the framework-UI / in-house-data bridge: when the framework owns both the UI primitives AND the data/transport layer (e.g., Mastra's `useChat` against `/api/agents/...` with its own thread storage), the user prefers using the framework end-to-end and reimplementing the in-house governance flows (approvals, elicitations) in the framework's layer rather than keeping the in-house data layer behind a shape adapter. The "framework UI + in-house data" hybrid is a downgrade compared to "framework UI + framework data + in-house governance as a reimplemented layer on top." The migration cost (re-implementing governance in Mastra) is accepted as part of the unconditional cutover. Concrete evidence: rewriting governed tools as individual Mastra tools with `requireApproval: true`, rewriting elicit as a Mastra tool using `suspend()`/`resume()`, and rejecting "papyrus_governed_tool" as a catch-all wrapper concept in favor of one-tool-per-governed-tool registration. Confidence: 0.95
- When the assistant falsely claims a framework lacks a primitive (e.g., "Mastra's useChat doesn't have a native flow" for elicitations), the user pushes back tersely with profanity AND pastes the relevant docs URLs as disproof. The pattern is: framework-native primitives almost always exist for the use case the assistant is reinventing — when tempted to build a custom governance/UI/data layer "because the framework doesn't have one", first verify against the framework's actual docs. Reachable examples so far: Mastra's `requireApproval` for tool calls, `suspend()`/`resume()` for elicitations, `useChat.approveToolCall`/`declineToolCall`/`isAwaitingToolApproval` on the React side. Confidence: 0.9
- When the assistant invents a wrapper/facade/abstraction layer (e.g., "papyrus_governed_tool" as a single tool that dispatches to all governed tools by name) to bridge in-house governance with a framework that has its own native primitives for the same concern, the user rejects the invention and demands using the framework's native primitives directly. The test: if the framework already has a way to express per-tool approval gating (Mastra's `requireApproval`), per-tool registry (one Mastra tool per governed tool name), and per-call execution dispatch (each tool's `execute` reading from `requestContext`), then registering one Mastra tool per governed tool is the right shape — not a single dispatcher tool. Confidence: 0.85
- Before committing to a UX/adoption redesign, the user expects a quick operational sanity check on the foundation they're moving onto ("We have the server working right?"). Confirm the framework adapter is wired, the auth propagation works, the relevant routes respond — *before* writing the new component. Treat "is the foundation actually up?" as the first question of any framework-driven UX work, not as something to discover mid-implementation. Confidence: 0.8
